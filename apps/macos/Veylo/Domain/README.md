# Domain

Pure Swift entities, value types and business rules. No SwiftUI, UIKit, AppKit, networking, persistence or global platform state. This layer has no dependency on the other application layers.
