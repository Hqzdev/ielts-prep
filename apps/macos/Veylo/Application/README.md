# Application

Use cases and protocols for required services. Depends only on Domain. Receive dependencies through initializers; leave HTTP, Keychain, file access and audio implementations to Infrastructure.
