# Infrastructure

Concrete adapters implementing Application protocols: API transport, Keychain, persistence and audio. Depends on Application and Domain, never on Interfaces. Composition constructs adapters.
